DATABASE_HOST: localhost
DATABASE_PORT: 5432
DATABASE_USER: postgres
DATABASE_USER_PASSWD: yourpasswd
DATABASE_DBNAME: fosm

logchannel: -1001206244890
daystoban: 365

immunity:
  - owner
  - superadmin
  - fedadmin

LEVELS:
  - owner
  - superadmin
  - fedadmin
  - user
  - banned

LEVEL_OWNER:
  - promote
  - demote
  - newowner

LEVEL_SUPERADMIN:
  - addgroup
  - removegroup
  - changelevel

LEVEL_FEDADMIN:
  - demoteme
  - fban
  - funban
  - fbanlist
  - userstat
  - changecomment
  - testme
  - search

LEVEL_USER:
  - fedadmins
  - superadmins
  - owners

LEVEL_BANNED:
  - mylevel
  - mystat
  - groupid
  - myid
  - mydata
  - help
  - start

adduser: insert into users values (%s,%s,%s,'user','',NULL,%s);
getuser: select * from users where id=%s;
removeuser: delete from users where id=%s;
changelevel: update users set level=%s where id=%s;
authorizegroup: insert into groups values (%s);
deauthorizegroup: delete from groups where id=%s;
getgroups: select id from groups;
getgroup: select id from groups where id=%s;
getusersbylevel: select id, username, comment, issuedbyid where level=%s;
getusersbydisplayname: select id, username, displayname, level from users where displayname like %s;
userexists: select * from users where id=%s LIMIT 1;
resolveusername: select id, username from users where username=%s LIMIT 1;
banuser: update users set level='banned' where id=%s;
unbanuser: update users set level='user' where id=%s;
updatecomment: update users set comment=%s where id=%s;
updatedisplayname: update users set displayname=%s where id=%s;
updateissuedbyid: update users set issuedbyid=%s where id=%s;
updatetimestamp: update users set timestamp=%s where id=%s;

table_users:
  - id int
  - username text
  - displayname text
  - level text
  - comment text
  - issuedbyid int
  - ts timestamp

table_groups:
  - id bigint
